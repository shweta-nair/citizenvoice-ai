# CitizenVoice AI — Python dependencies
# Install with: pip install -r requirements.txt

streamlit>=1.36.0          # st.navigation / st.Page multipage API used by app.py
pandas>=2.0.0
scikit-learn>=1.3.0
plotly>=5.18.0
reportlab>=4.0.0
joblib>=1.3.0
nltk>=3.8.0                # listed per project spec; not required at runtime
                            # (utils/preprocessing.py uses a built-in stopword
                            # list so the app never needs nltk.download())
